#[derive(Drop, Copy)]
struct Point {
    x: u128,
    y: u128,
}

fn main(){
    let p1 = Point {x: 5, y: 10};
    foo(p1); //instead of passing ownership of p1 to foo, we are passing a copy of p1 to foo, so p1 can still be used after this line
    foo(p1);
}

fn foo(p: Point){  //do something with p
}